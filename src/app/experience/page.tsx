import React from "react";
import ExperienceClient from "../components/ExperienceClient";

export const metadata = {
  title: "Experience - Cam Ziny",
};

const Experience = () => {
  return <ExperienceClient />;
};

export default Experience;
